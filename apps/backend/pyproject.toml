[project]
name = "evolvai-backend"
version = "0.1.0"
description = "EvolvAI Step 1 backend foundation"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
